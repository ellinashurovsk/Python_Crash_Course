terms = {
    "List": "the list is a type of container in Data Structures, which is used to store multiple data at the same time.",
    "Tuple": "a sequence data type that can contain elements of different data types, but these are immutable in nature.",
    "Dictionary": "an associative array that holds key-value pairs.",
    "Argument": "a value we pass to a function or a method when calling it.",
    "Range()": "returns a list of integers, the sequence of which is defined by the arguments passed to it.",
    "A data type": "is the classification of a particular type of data.",
    "Arrays": "are lists or groups of similar types of data values that are grouped.",
    "Declaration": "A statement that describes a variable, function or any other identifier is called a declaration.",
    "A loop": "a sequence of instructions that repeat the same process over and over until a condition is met and it receives the order to stop"
}

print("\n\t\tGLOSSARY:\n")
for keys, values in terms.items():
    print(f"\n{keys}-")
    print(f"{values}")
